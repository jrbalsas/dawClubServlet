<?xml version="1.0" encoding="UTF-8"?>
<Context antiJARLocking="true" path="/dawClubServlet">
  <!--Apache Tomcat Derby Connection Pool-->
  <!-- TOMCAT Datasource definition (see glassfish-resources.xml) -->
  <!--
  <Resource auth="Container" driverClassName="org.apache.derby.jdbc.ClientDriver" maxTotal="8" name="jdbc/gestClub" password="dawuser1" type="javax.sql.DataSource" url="jdbc:derby://localhost:1527/club" username="dawuser1"/>
  -->

  <!-- Tomcat Authentication Realm config. Select only one of them -->
  <!-- Tomcat File Realm -->
  <!-- 
  <Realm className="org.apache.catalina.realm.UserDatabaseRealm" resourceName="UserDatabase"/>
  -->  
  <!-- Tomcat JDBC Realm-->
  <!--
  <Realm className="org.apache.catalina.realm.DataSourceRealm" 
        dataSourceName="jdbc/gestClub" localDataSource="true" 
        roleNameCol="rol" 
        userCredCol="clave" 
        userNameCol="usuario" 
        userRoleTable="roles" 
        userTable="usuarios"/>
   -->
</Context>
